import React from 'react';

const Navigation = () => {
    return(
        <nav className="flex justify-end">
            <p className="text-lg underline p-4 cursor-pointer hover:text-gray-500">Sign Out</p>
        </nav>
    )
}

export default Navigation;